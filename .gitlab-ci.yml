before_script:
    - apt-get update && apt-get install -y python3-sphinx python3-sphinxcontrib.spelling

build_job:
    script:
        - cd en
        - make clean
        - make html
    artifacts:
        - en/build/html
